#pragma once

class Fl_Widget;

namespace Kashyyyk {

// Opens the serverlist. This can connect to servers directly, as well as
// change per-server settings.
//
void ServerList(Fl_Widget *w, /* Kashyyyk::Window */ void *p);

}
